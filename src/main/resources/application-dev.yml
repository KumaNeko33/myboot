# 开发环境配置
spring:
# 数据源配置，请修改为你项目的实际配置
  datasource:
    url: jdbc:mysql://localhost:3306/sso?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=utf-8&autoReconnect=true&useSSL=true
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
#    跟踪sql用的，便于调试
  jpa:
    show-sql: true